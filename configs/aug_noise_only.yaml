# Ablation: gaussian noise only (all other augmentations disabled).
# Parameters match the current data_augmentation.yaml baseline.

gaussian_noise:
  p: 1.0
  sigma: 14
    # type: uniform
    # low: 3.0
    # high: 9.0
  delta_p: 1.0

rotation_scaling:
  p: 0.0
  angle: 15.0
  scale_range: [0.9, 1.1]

simulated_low_res:
  p: 0.0
  factor_range: [1, 2]

mirroring:
  p: 0.0

gaussian_blur:
  p: 0.0
  kernel_size: 5
  sigma: 14
